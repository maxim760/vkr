import React from 'react'
import { Layout } from 'src/components/ui/Layout/layout/Layout'

interface IProps {
  
}

export const OrderPage: React.FC<IProps> = ({ }) => {
  
  return (
    <Layout title="Заказ">
      sign OrderPage
    </Layout>
  )
}